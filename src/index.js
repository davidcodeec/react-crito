import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));

 {/* This is where you put the components e.g App.js which contains code. A way to write a comment in react too */}

root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

